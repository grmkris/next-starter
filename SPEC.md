# Product Specification — TODO: Project Name

## Overview

TODO: Brief product description.

## Architecture

Single Next.js app with:
- `app/(app)/*` — Authenticated user interface
- `app/(public)/*` — Public pages (login, landing)
- `app/api/*` — API endpoints (oRPC + Better Auth)

## Domains

TODO: List domain areas. Each becomes a schema folder + service + router.

Example:
- **auth** — User accounts, sessions (included by default)
- **todo** — Task management (example domain to add)

## Build Phases

### Phase 1 — MVP

TODO: List core features.

### Phase 2 — Expand

TODO: List next features.
